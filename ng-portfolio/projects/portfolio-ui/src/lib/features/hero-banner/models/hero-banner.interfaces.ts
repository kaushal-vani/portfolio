import { Button, Card, Logo } from "portfolio-shared/src";

export interface HeroBannerContent {
  title: string;
  description: string;
  heroBannerImage: Logo;
  downloadButton: Button;
  toast:Card;
}
